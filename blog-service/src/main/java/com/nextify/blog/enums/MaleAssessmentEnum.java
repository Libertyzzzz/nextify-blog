package com.nextify.blog.enums;

import lombok.Getter;

@Getter
public enum MaleAssessmentEnum {
    M01("顶级印钞机", "硬件满格，资产通天。你是这个维度的终极物种，唯一的风险是对方会怀疑你是杀猪盘。", 90),
    M02("傲慢杠杆男", "经济实力雄厚但脾气极差。你不是在找伴侣，你是在找一个能忍受高压环境的首席执行官。", 80),
    M03("绩优原始股", "收入上升期且审美在线。现在的你还没到巅峰，但聪明人已经开始排队申购你了。", 70),
    M04("金融伪装者", "视觉身高180，实际172。报表注水严重，建议先去把鞋垫拿出来，坦诚面对二级市场。", 60),
    M05("高产程序员", "钱多话少逻辑死。你像个稳定的服务器，虽然永不宕机，但也毫无情趣可言。", 65),
    M06("精致负债人", "审美极高，穿搭极帅，余额极惨。你是典型的“社交奢侈品”，只能租借，不能持有。", 55),
    M07("妈宝避风港", "硬件还行，但你的独立决策权仅限中午吃什么。建议带你妈一起去约会，效率更高。", 50),
    M08("无敌血包", "赚钱能力不错，但你是个漏水的容器。资产会被家族结构瞬间稀释，建议单修。", 55),
    M09("过气校草", "曾经颜值巅峰，现在发量报警。你的估值正在随发际线一起崩盘，建议止损。", 60),
    M10("气氛组组长", "会接梗、会拍照、没存款。你是完美的“短期租赁项目”，持有的风险是吃不饱饭。", 50),
    M11("冷暴力专家", "心理架构极其闭塞。冲突时你比离线的路由器还难找，售后维护成本极大。", 45),
    M12("大龄巨婴", "虽有年薪，但生活不能自理。你需要的不是女朋友，是一个带薪入组的顶级保姆。", 40),
    M13("无效健身男", "只有肌肉，没有存款，且极度自恋。你的核心资产是你的背影，建议只留背影。", 45),
    M14("普通且自信", "算法显示数据全靠主观粉饰。你对自我的认知偏差，已超出人类医学范畴。", 35),
    M15("备胎专业户", "温柔、随和、没钱。你是完美的系统插件，只有当主程序报错时，别人才会想起你。", 40),
    M16("社交边缘人", "存在感极低，经济基础薄弱。你在市场上几乎处于“停盘”状态，建议先去开户。", 30),
    M17("画饼艺术家", "满脑子宏大叙事，兜里没有钢板。你的情怀不能变现，建议回火星创业。", 35),
    M18("油腻中年预备役", "年纪轻轻已放弃身材管理，审美停留在上个世纪。你正在加速通往油腻。", 30),
    M19("二次元死宅", "只对纸片人深情，现实社交能力近乎为零。你的世界不需要对象，需要充电宝。", 25),
    M20("键盘侠战神", "现实里唯唯诺诺，网络上重拳出击。你的核心战力全在指尖，极度危险（指智商）。", 20),

    M21("低配版霸总", "由于看了太多的职场网文，导致你说话总是带着一种尴尬的命令语气。资产不足，爹味有余。", 35),

    M22("资深钓鱼佬", "除了鱼，你对这个世界的一切都不感兴趣。你的情感带宽已被那根鱼竿占满，建议找条美人鱼。", 25),

    M23("无效社交达人", "认识的人很多，但能借到钱的一个没有。你的估值全靠那一叠没用的名片在支撑。", 30),

    M24("视觉欺诈师", "发帖全是豪车方向盘，私下里在挤地铁。你这种高杠杆运作的虚假资产，随时有爆仓风险。", 20),

    M25("爹系男友预备役", "习惯性教人做事。你的同理心被说教欲完全覆盖，建议去应聘初中班主任，别在相亲市场浪费时间。", 35),

    M26("沉没成本收割机", "极度吝啬且计算精准。约会喝瓶水都要AA，你不是在找对象，你是在找一个共同承担公摊面积的室友。", 28),

    M27("妈宝男2.0", "核心逻辑是“我妈说”。你没有独立的算法，只是一个运行了三十年的家庭脚本镜像。", 30),

    M28("夜店打火机", "看起来很爱社交，实际只会蹦迪。你的核心价值仅存在于卡座和酒精中，天亮即作废。", 25),

    M29("怀才不遇型选手", "认为世界亏欠了你，实际上是你亏欠了世界一份努力。你的负能量磁场足以劝退所有优质资产。", 32),

    M30("资产重组对象", "经济、颜值、性格三项基本面全部触发熔断。建议退出市场，回炉重造。", 0);

    private final String label;
    private final String content;
    private final int minScore;

    MaleAssessmentEnum(String label, String content, int minScore) {
        this.label = label;
        this.content = content;
        this.minScore = minScore;
    }

    public static MaleAssessmentEnum getResult(double score, int stub, long income, int aesthetic) {
        // 1. 极端特质判定 (优先级最高：这些特质会直接定义一个人)

        // 顶级资产 vs 顶级风险
        if (income >= 1500000 && score >= 85) return M01; // 顶级印钞机
        if (income >= 800000 && stub > 8) return M02;    // 傲慢杠杆男 (有钱但极度难搞)

        // 性格缺陷类
        if (stub > 8) {
            if (income < 150000) return M21; // 低配版霸总 (没钱还爱命令)
            if (score < 50) return M25;      // 爹系男友预备役 (习惯性教人做事)
            if (score >= 60) return M11;     // 冷暴力专家 (心理闭塞)
        }

        // 视觉/审美欺诈类
        if (aesthetic >= 8) {
            if (income < 100000) return M06; // 精致负债人 (穷帅典范)
            if (score < 50) return M24;      // 视觉欺诈师 (豪车方向盘是借的)
            if (score < 60) return M10;      // 气氛组组长 (只适合短期收割)
        }

        // 2. S级/A级群组 (Score >= 75): 优质或潜力资产
        if (score >= 75) {
            if (income > 400000 && aesthetic >= 6) return M03; // 绩优原始股
            if (stub <= 4) return M05;                         // 高产程序员 (稳重、低调、话少)
            return M08;                                        // 无敌血包 (赚钱但被家族稀释)
        }

        // 3. 维度特征分流 (核心优化：根据特质决定标签)

        // --- 妈宝/独立性维度 ---
        // (假设我们在 DTO 中有相关参数，这里用 hash 或 stub 模拟判定)
        if (stub < 3 && score < 60) {
            if (income > 200000) return M07; // 妈宝避风港 (有钱但没主见)
            return M27;                      // 妈宝男2.0
        }

        // --- 审美与生活方式 ---
        if (aesthetic < 3) {
            if (score > 50) return M05;      // 典型程序员 (无审美但有产出)
            if (score < 40) return M18;      // 油腻中年预备役
            return M19;                      // 二次元死宅
        }

        // --- 情感定位 ---
        if (stub <= 4 && income < 120000) {
            if (aesthetic >= 6) return M15;  // 备胎专业户 (温柔没钱)
            return M26;                      // 沉没成本收割机 (精于计算)
        }

        // --- 特殊爱好触发 (基于随机因子与中低分)
        if (score < 50) {
            int hobbyTrigger = (int) (score + aesthetic) % 5;
            if (hobbyTrigger == 0) return M22; // 资深钓鱼佬
            if (hobbyTrigger == 1) return M13; // 无效健身男
            if (hobbyTrigger == 2) return M28; // 夜店打火机
        }

        // 4. 稳健性分类 (45-74)
        if (score >= 60) {
            if (aesthetic >= 6) return M09;  // 过气校草 (颜值在崩盘)
            if (income > 250000) return M05; // 稳健程序员
            return M23;                      // 无效社交达人
        }

        if (score >= 45) {
            if (stub > 7) return M29;        // 怀才不遇型 (负能量)
            if (aesthetic > 5) return M04;   // 金融伪装者 (注水严重)
            return M14;                      // 普通且自信
        }

        // 5. 底部清算 (兜底)
        if (score < 30) return M30;          // 资产重组对象 (熔断)

        // 剩余散列
        int finalHash = (int) (score) % 5;
        MaleAssessmentEnum[] lowValues = {M16, M17, M20, M14, M18};
        return lowValues[finalHash];
    }
}