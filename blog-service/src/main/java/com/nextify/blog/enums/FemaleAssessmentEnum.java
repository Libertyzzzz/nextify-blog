package com.nextify.blog.enums;

import lombok.Getter;

@Getter
public enum FemaleAssessmentEnum {
    F01("顶级稀缺资产", "审美、稳定性与边界感都很强。你在新模型里属于高分段且波动低的稀缺样本。", 90),
    F02("高审美高转化", "外在表达和社交转化效率都不错。你不仅有展示力，也有持续连接能力。", 80),
    F03("硬核资源主导型", "资源能力突出，但个性强势。高配关系需要更高的匹配阈值。", 75),
    F04("高共情高策略", "同理心与判断力同时在线。你能把关系质量和长期收益兼顾得很好。", 85),
    F05("高吸引高维护", "外在竞争力强，但情绪管理波动较大。模型会给高吸引分，也会计入维护成本。", 75),
    F06("田园小清新", "性格温婉，BMI健康。你是市场的“避险资产”，虽然不暴利，但能让人多活几年。", 70),
    F07("形象运营型", "展示能力很强，但线下一致性仍有提升空间。模型会重点看长期兑现率。", 60),
    F08("认知压制型", "逻辑和表达很强，但互动容错偏低。高质量匹配需要更强的双向沟通。", 65),
    F09("恋爱脑战神", "颜值尚可但智商在恋爱时归零。你是一笔极其容易被收割的优质资产。", 55),
    F10("大龄叛逆少女", "拒绝向年龄妥协，拒绝向市场低头。虽然估值在下降，但你依然坚持溢价。", 60),
    F11("低冲突协同型", "配合度高、冲突低，但自主决策权相对弱。适合在边界建设后继续增值。", 50),
    F12("猫系小作精", "只有在你想分享废话的时候才存在。你的分享欲是对方的手机内存杀手。", 55),
    F13("野生艺术家", "穿搭古怪，性格孤僻。你不是在寻找伴侣，你是在寻找另一个外星信号。", 45),
    F14("中位稳态样本", "各维度接近中位，短板不明显但记忆点不足。适合靠单项突破拉开分差。", 45),
    F15("退役女神", "曾经众星捧月，现在回归人间。预期还留在高点，但成交量已萎缩。", 55),
    F16("吸渣体质", "审美不错但眼光极差。你就像个专门收集残次品的仓库，建议及时清理库存。", 40),
    F17("自愈系打工人", "除了上班就是睡觉，生活极度枯燥。你的灵魂由于长期加班已失去弹性。", 35),
    F18("精致土代表", "用力过猛的穿搭，廉价感的审美。你对潮流的理解似乎存在某种生物学屏障。", 30),
    F19("情绪波动高风险", "情绪稳态和关系可得性波动较大，会在新模型中被连续惩罚。", 35),
    F20("追星专业户", "把所有情感寄托在偶像身上。现实里的男性在你眼里都是“垃圾”。", 30),

    F21("田园诗织梦者", "满脑子逃离城市，向往诗和远方。你的核心资产是你的想象力，核心风险是无法适应现实的粗糙。", 35),

    F22("打卡机器", "每周末都在网红店的路上。你对生活的感知力完全取决于取景框和构图，是一个活在相册里的数据包。", 30),

    F23("高外向高压迫", "社交能量很高，但容易让关系失去边界。需要平衡热情与对方空间。", 35),

    F24("极度省钱专家", "消费观极其保守，为了攒钱拒绝一切审美溢价。你活得像一串精密的会计分录，安全但枯燥。", 25),

    F25("玄学信徒", "开口水逆和闭口塔罗。你的命运不掌握在自己手里，掌握在那个每天更新的星座博主手里。", 30),

    F26("隐形鸽王", "分享欲极强但行动力为零。所有约会都止步于“改天”，你的信用评级在社交圈已降至垃圾级。", 28),

    F27("赛博隐士", "社交媒体零更新，朋友圈三天可见。你这种“离线状态”让异性无法定位，属于沉默资产。", 32),

    F28("无效健身媛", "办了年卡只为了在落地镜前拍三张照。你的核心战力全在修图软件的“液化”功能里。", 25),

    F29("高标准筛选型", "判断力和边界感很强，筛选效率高。优势是避险能力，代价是匹配窗口更窄。", 40),
    F30("生物安全线熔断", "生理健康或外在管理触发底线约束，当前处于模型保护区。", 0);


    private final String label;
    private final String content;
    private final int minScore;

    FemaleAssessmentEnum(String label, String content, int minScore) {
        this.label = label;
        this.content = content;
        this.minScore = minScore;
    }

    public static FemaleAssessmentEnum getResult(double score, int aesthetic, double bmi, int stub, long income) {
        // 1. 极端生存属性判定 (优先级最高，一票否决)
        if (bmi > 28) return F30; // 视觉灾难
        if (income > 1000000 && stub > 8) return F03; // 硬核女霸总：极高收入+极高任性

        // 2. S级群组 (Score > 85): 顶级稀缺资产
        if (score >= 85) {
            if (aesthetic >= 5) return F01;      // 审美顶尖 -> 黑月亮
            if (stub <= 4) return F04;           // 性格极好 -> 有效绿茶(褒义)
            if (stub > 8) return F05;            // 难搞 -> 高维护费女神
            return F29;                          // 逻辑派 -> 人间清醒
        }

        // 3. A级群组 (70-84): 优质流通资产
        if (score >= 70) {
            if (aesthetic >= 4) return F02;      // 氛围感玩家
            if (bmi >= 19 && bmi <= 22) return F06; // 健康美 -> 田园小清新
            if (stub > 7) return F08;            // 毒舌才女 -> 知识分子
            if (income > 500000) return F03;     // 经济独立 -> 女霸总(轻量版)
            return F29;
        }

        // 4. 维度特质分流 (核心优化：不看分数，看你哪项最突出)

        // --- 审美/虚荣维度 ---
        if (aesthetic >= 4) {
            if (score > 50) return F07;          // 朋友圈雕琢者
            if (bmi < 20) return F28;            // 健身媛
            return F22;                          // 打卡机器
        }

        // --- 性格/情绪维度 ---
        if (stub >= 8) {
            if (score > 55) return F12;          // 猫系小作精
            if (score > 40) return F13;          // 野生艺术家
            return F19;                          // 深夜emo患者
        }

        // --- 经济/生存维度 ---
        if (income < 120000) { // 深圳标准下的低收入敏感
            if (stub > 7) return F19;            // 穷且emo
            return F17;                          // 自愈系打工人
        }

        // --- 认知/行为维度 ---
        if (stub < 4) {
            if (score < 50) return F09;          // 恋爱脑战神
            return F11;                          // 乖乖女插件
        }

        // 5. 剩余样本的中位散列 (45-69)
        if (score >= 45) {
            if (bmi > 24) return F15;            // 退役女神
            if (aesthetic < 4) return F18;       // 精致土
            return F14;                          // 平平无奇
        }

        // 6. 底部兜底与奇葩分类
        int hash = (int) (score + stub + aesthetic) % 8;
        FemaleAssessmentEnum[] chaoticValues = {F10, F16, F20, F21, F23, F24, F25, F26};
        return chaoticValues[hash];
    }
}
